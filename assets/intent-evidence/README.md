<!--
Intent-review evidence images. Drop the 5 screenshots here with EXACTLY these
filenames — the hidden page /intent-evidence/ and the application links depend
on them:

  server-members-1-join.png            — a member-join event being handled
  server-members-2-role-tiers.png      — /setup role -> Mod/Admin/Owner tier mapping
  presence-stats.png                   — /stats showing live Online + In-voice counts
  message-content-1-automod-removal.png— AutoMod removing a filtered message
  message-content-2-message-report.png — right-click Message Report summary

PNG or JPG is fine, but keep the .png name (or update the page + application to
match). Unlisted page: not in nav, search, or sitemap; blocked in robots.txt.
-->
